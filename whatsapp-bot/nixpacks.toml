[phases.setup]
# Removed tesseract - using tesseract.js which bundles its own WASM implementation
nixPkgs = []

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

